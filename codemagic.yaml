workflows:
  android-production:
    name: ZOLD Android Production
    max_build_duration: 60
    environment:
      # Pinned, not `stable`: current Flutter stable requires Gradle >= 9.1 / AGP >= 9.0.1 /
      # Kotlin >= 2.2.20, while this project targets Gradle 8.11.1 / AGP 8.9.1 / Kotlin 2.1.0.
      # 3.35.6 satisfies pubspec.lock's Flutter >=3.35.6 / Dart >=3.9.2 requirements while
      # remaining on the existing Android toolchain. Upgrading beyond this line means
      # upgrading all three of those together (and handling the AGP 9 "new DSL" migration).
      flutter: 3.35.6
      groups:
        - zold_env          # holds BASE_URL, RAZORPAY_KEY, RISK_DISCLOSURE_PDF_URL, CONNECT_TIMEOUT, RECEIVE_TIMEOUT, PRIVACY_POLICY_URL, WEB_URL
        - zold_android_signing  # holds CM_KEYSTORE, CM_KEYSTORE_PASSWORD, CM_KEY_ALIAS, CM_KEY_ALIAS_PASSWORD
        - zold_play_credentials  # holds GCLOUD_SERVICE_ACCOUNT_CREDENTIALS (Play Console service account JSON)
      vars:
        PACKAGE_NAME: "in.zold.app"
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
      tag_patterns:
        - pattern: "v*.*.*"
          include: true
    scripts:
      - name: Write .env from secure variables
        script: |
          cat <<EOF > "$CM_BUILD_DIR/.env"
          BASE_URL=$BASE_URL
          RAZORPAY_KEY=$RAZORPAY_KEY
          RISK_DISCLOSURE_PDF_URL=$RISK_DISCLOSURE_PDF_URL
          CONNECT_TIMEOUT=$CONNECT_TIMEOUT
          RECEIVE_TIMEOUT=$RECEIVE_TIMEOUT
          PRIVACY_POLICY_URL=$PRIVACY_POLICY_URL
          WEB_URL=$WEB_URL
          EOF

      - name: Set up Android signing
        script: |
          echo "$CM_KEYSTORE" | base64 --decode > "$CM_BUILD_DIR/android/app/keystore.jks"
          cat <<EOF > "$CM_BUILD_DIR/android/key.properties"
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_ALIAS_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=keystore.jks
          EOF

      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Set build number from Play Console
        script: |
          LATEST_BUILD_NUMBER=$(google-play get-latest-build-number \
            --package-name "$PACKAGE_NAME" \
            --credentials "$GCLOUD_SERVICE_ACCOUNT_CREDENTIALS" \
            --tracks="internal")
          echo "Latest build number on internal track: $LATEST_BUILD_NUMBER"
          echo "NEXT_BUILD_NUMBER=$((LATEST_BUILD_NUMBER + 1))" >> $CM_ENV

      - name: Build AAB
        script: |
          flutter build appbundle --release --build-number=$NEXT_BUILD_NUMBER
    artifacts:
      - build/app/outputs/**/*.aab
      - build/app/outputs/**/mapping.txt
    publishing:
      email:
        recipients:
          - dme@botivate.in
        notify:
          success: true
          failure: true
      google_play:
        credentials: $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS
        track: internal          # promote to production manually in Play Console once ready
        submit_as_draft: true
